import ReflectionWorkspaceClient from './ReflectionWorkspaceClient';

export const metadata = { title: 'Reflection · Loom' };

export default function ReflectionPage() {
  return <ReflectionWorkspaceClient />;
}
